<!-- ADOPT — once, instead of CONSTITUTION, when the codebase already exists. -->

We have an existing project: <project name>, <one-line description>.
Read the codebase, docs, TODOs and git history, then create a "constitution"
in a specs directory:
- mission.md and tech-stack.md reverse-engineered from what is actually there
- roadmap.md with what already exists as completed phases, then future work in
  very small phases built from the TODOs/backlog.

Important: You *must* ask me clarifying questions, grouped on these 3, about
the gaps the code can't answer, before writing to disk.
